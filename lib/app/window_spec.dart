/// 桌面原型的固定画布规格，原生启动代码也应使用同一组数值。
abstract final class DesktopWindowSpec {
  /// 应用标题。
  static const title = 'sendreq';

  /// 画布宽度。
  static const width = 1440.0;

  /// 画布高度。
  static const height = 900.0;
}
